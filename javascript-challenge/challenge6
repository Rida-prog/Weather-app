// create a function that incerements the lapsCompleted variable with one
// run it three times
 let lapsCompleted = 0;

function myIncrementLaps() {
      lapsCompleted = lapsCompleted + 1;
}

myIncrementLaps();
myIncrementLaps();
myIncrementLaps();

console.log(lapsCompleted);